version: "3"

services:
  web:
    container_name: web
    build: .
    command: python manage.py runserver 0.0.0.0:8000
    # command: /tmp/server_run.sh
    ports:
      - "8000:8000"
    volumes:
      - django_dev:/app
    environment:
      - POSTGRES_NAME=${POSTGRES_NAME}
      - POSTGRES_USER=${POSTGRES_USER}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
    depends_on:
      - db

  db:
    container_name: db
    image: postgres:13
    pull_policy: always
    volumes:
      - postgres_data:/var/lib/postgresql/data/
    environment:
      - POSTGRES_DB=${POSTGRES_NAME}
      - POSTGRES_USER=${POSTGRES_USER}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}

volumes:
  postgres_data:
    name: postgres_data

  django_dev:
    name: django_dev
    driver: local
    driver_opts:
      o: bind
      type: none
      device: ${PWD}

networks:
  default:
    name: django_network
